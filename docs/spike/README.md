# Spike archive

Research snapshot from the feasibility spike (repo: `longhand`). Nothing
here is built, shipped, or linted: `src/` is outside the plugin's tsconfig
and esbuild entry point, and it leans on internal API deliberately, as the
experiments required. It stays in the repo as the evidence behind
`FINDINGS.md`, which the plugin's README cites.
